apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: deploy-ingress
  annotations:
    ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - http:
        paths:
          - path: /
            backend:
              serviceName: my-deploy
              servicePort: 8081