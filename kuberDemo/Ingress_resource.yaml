apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-ingress
  namespace: default
spec:
  rules:
    - host: localhost
    - http:
        paths:
          - backend:
              service:
                name: localhost
                port:
                  number: 8081
            pathType: Prefix
            path: /